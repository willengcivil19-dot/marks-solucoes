# Envio do formulário de contato por e-mail (opcional).
# Sem essas variáveis, o formulário continua funcionando normalmente
# (o lead é apenas registrado no log do servidor).
RESEND_API_KEY=
EMAIL_FROM="Marks Soluções <contato@seu-dominio.com.br>"
